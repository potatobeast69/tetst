# Быстрый старт - 5 минут до запуска

## Что у вас есть

✅ Директория `template/` с полностью готовым Git Template
✅ 3 CLI инструмента в текущем репозитории (SwiftCodeReviewTools)
✅ Workflow который автоматически запускает все проверки

## Что нужно сделать

### Шаг 1: Создайте Template Repository (2 минуты)

```bash
# 1. Перейдите в директорию template
cd template

# 2. Инициализируйте git
git init

# 3. Сделайте первый commit
git add .
git commit -m "Initial template setup"

# 4. Создайте репозиторий на GitHub
# Название: SwiftProjectTemplate (или любое другое)

# 5. Свяжите с remote и push
git branch -M main
git remote add origin https://github.com/YOUR_ORG/SwiftProjectTemplate.git
git push -u origin main
```

### Шаг 2: Сделайте его Template (1 минута)

1. Откройте репозиторий на GitHub
2. Перейдите в **Settings**
3. Отметьте ☑️ **"Template repository"**
4. Сохраните

Теперь появится кнопка **"Use this template"**!

### Шаг 3: Обновите ссылку на Tools (1 минута)

В файле `.github/workflows/code-review.yml` найдите строку ~46:

```yaml
git clone https://github.com/${{ github.repository_owner }}/SwiftCodeReviewTools.git tools
```

Замените `SwiftCodeReviewTools` на реальное название вашего репозитория с инструментами.

**Пример:**
```yaml
git clone https://github.com/${{ github.repository_owner }}/dodelivaem.git tools
```

Также обновите в `scripts/setup.sh` строку ~62:

```bash
git clone https://github.com/YOUR_ORG/dodelivaem.git "$TOOLS_DIR"
```

### Шаг 4: Commit и push изменения (30 секунд)

```bash
git add .github/workflows/code-review.yml scripts/setup.sh
git commit -m "Update tools repository URL"
git push
```

### Шаг 5: Готово! Тестируйте (30 секунд)

Теперь студенты (или вы для теста) можете:

1. Нажать **"Use this template"** на GitHub
2. Создать новый репозиторий
3. Клонировать его
4. Скопировать Example проект:
   ```bash
   cp -r Example/* .
   git add .
   git commit -m "Add example project"
   git push
   ```
5. Перейти в **Actions** → увидеть результаты!

## Как это выглядит для студентов

### 1. Создание проекта

```
GitHub → SwiftProjectTemplate → "Use this template"
→ Создается их репозиторий с workflow
```

### 2. Добавление кода

```bash
git clone https://github.com/student/their-project.git
cd their-project
# добавить свой Swift код
git add .
git commit -m "Initial code"
git push
```

### 3. Просмотр результатов

```
GitHub → их репозиторий → Actions
→ Видят все 4 проверки с результатами
```

### 4. Скачивание отчетов (опционально)

```
Actions → выбрать запуск → прокрутить вниз
→ Artifacts → скачать JSON отчеты
```

## Пример вывода в консоли

После push студент увидит:

```
╔════════════════════════════════════════════════════════╗
║          📊 СВОДНЫЙ ОТЧЕТ CODE REVIEW                  ║
╚════════════════════════════════════════════════════════╝

📌 Репозиторий: student/my-swift-app
🔀 Ветка: main
👤 Автор: student_username
📝 Commit: abc123def456

✅ 🔨 Сборка: УСПЕШНО
🎨 Стиль кода: 2 ошибок, 15 предупреждений
🗑️  Неиспользуемый код: 3 элемента
💾 Утечки памяти: 0 ошибок, 5 предупреждений

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Интеграция с backend (опционально)

Если хотите автоматически получать результаты на ваш backend:

### 1. Создайте API endpoint

```python
# Пример на Flask
@app.route('/api/code-review/results', methods=['POST'])
def receive_results():
    data = request.json
    # сохранить в БД
    return {'status': 'ok'}, 200
```

### 2. Добавьте secrets

В настройках каждого студенческого репозитория:
- Settings → Secrets → Actions
- Добавьте:
  - `BACKEND_URL` = `https://your-backend.com/api/code-review/results`
  - `BACKEND_TOKEN` = `your_secret_token`

**Совет:** Используйте Organization secrets если все в одной организации.

### 3. Раскомментируйте код

В `.github/workflows/code-review.yml` строки ~345-350:

```yaml
# Было закомментировано
# if [ -n "${{ secrets.BACKEND_URL }}" ]; then
#   curl -X POST ${{ secrets.BACKEND_URL }} \
#     ...
# fi

# Сделайте
if [ -n "${{ secrets.BACKEND_URL }}" ]; then
  curl -X POST ${{ secrets.BACKEND_URL }} \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer ${{ secrets.BACKEND_TOKEN }}" \
    -d @final-report.json
fi
```

### 4. Push изменения

```bash
git add .github/workflows/code-review.yml
git commit -m "Enable backend integration"
git push
```

## Проверка что всё работает

### Тест 1: Локальная сборка Tools

```bash
cd /path/to/dodelivaem  # ваш репозиторий с инструментами
swift build -c release

# Должно успешно собраться
ls .build/release/
# → swift-style-check
# → swift-dead-code
# → swift-memory-check
```

### Тест 2: Workflow syntax

```bash
cd template
# Проверить YAML
yamllint .github/workflows/code-review.yml
# или
cat .github/workflows/code-review.yml | grep -E "^[a-z]"
```

### Тест 3: Создать тестовый репозиторий

1. Use template → создать test-repo
2. Скопировать Example
3. Push
4. Проверить Actions - должны запуститься

### Тест 4: Backend (если настроили)

```bash
# Сделайте тестовый запрос
curl -X POST https://your-backend.com/api/code-review/results \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "repository": "test/test",
    "branch": "main",
    "commit": "test123",
    "author": "test",
    "timestamp": "2024-01-01T00:00:00Z",
    "build_status": "success",
    "reports": {}
  }'

# Должно вернуть 200 OK
```

## Что если что-то не работает?

### Workflow не запускается

✅ **Решение:**
1. Убедитесь что Actions включены: Settings → Actions → "Allow all actions"
2. Проверьте что push в ветку `main` или `develop`
3. Проверьте что файл в `.github/workflows/code-review.yml`

### Не собираются Tools

✅ **Решение:**
1. Проверьте URL репозитория в workflow
2. Убедитесь что репозиторий публичный
3. Проверьте что `Package.swift` корректный

### SwiftLint ошибки

✅ **Решение:**
1. Настройте `.swiftlint.yml` - сделайте правила мягче
2. Или добавьте `disabled_rules` для проблемных правил
3. Или используйте `--strict false`

### Periphery не находит схему

✅ **Решение:**
1. Periphery работает только для Xcode проектов (.xcodeproj/.xcworkspace)
2. Для SPM эта проверка автоматически пропускается
3. Или укажите схему явно в workflow

## Дальнейшие шаги

После успешного запуска:

1. ✅ Дайте студентам инструкцию (template/README.md)
2. ✅ Проведите демо использования
3. ✅ Настройте backend (если нужно)
4. ✅ Добавьте дополнительные проверки (тесты, coverage)
5. ✅ Настройте автоматический grading

## Полезные команды

```bash
# Посмотреть структуру template
ls -la template/

# Проверить workflow файл
cat template/.github/workflows/code-review.yml | head -50

# Тест локального setup скрипта
cd template
./scripts/setup.sh

# Клонировать свой template для теста
git clone https://github.com/YOUR_ORG/SwiftProjectTemplate.git test-student-repo
```

## Документация

Для детального изучения смотрите:

- **TEMPLATE_README.md** - общий обзор решения
- **template/README.md** - инструкция для студентов
- **template/SETUP_GUIDE.md** - подробная инструкция для вас
- **template/SUMMARY.md** - краткое резюме
- **ARCHITECTURE.md** - архитектура системы

## Готово! 🎉

Теперь у вас работающий Git Template для автоматической проверки Swift проектов!

**Время настройки:** 5 минут
**Время на студента:** 0 минут (всё автоматически)
**Поддержка:** минимальная (всё работает само)

Вопросы? Смотрите документацию выше или логи GitHub Actions.
